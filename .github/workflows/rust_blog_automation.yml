name: Rust-Powered Blog Automation

on:
  schedule:
    # Run daily at 8 AM UTC
    - cron: '0 8 * * *'
  workflow_dispatch:
    inputs:
      tier:
        description: 'Gem tier to scan (micro/small/medium)'
        required: true
        default: 'small'
        type: choice
        options:
          - micro
          - small
          - medium

jobs:
  rust-blog-pipeline:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GH_PAT }}

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Setup Rust
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable
          override: true

      - name: Cache Rust dependencies
        uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            rust-scanner/target/
          key: ${{ runner.os }}-cargo-rust-scanner-${{ hashFiles('**/Cargo.lock') }}

      - name: Build Rust Scanner
        run: |
          cd rust-scanner
          cargo build --release --bin hidden-gems-scanner
          ls -lh target/release/

      - name: Create Output Directory
        run: mkdir -p output

      - name: 1. Run Rust Scanner
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          python scripts/run_rust_scanner_wrapper.py ${{ github.event.inputs.tier || 'small' }} output/rust_scan.json

      - name: 2. Bridge to AI Format
        run: |
          python scripts/bridge_rust_to_ai.py output/rust_scan.json output/bridge_scan.json

      - name: 3. Run AI Review (Gemini with key rotation)
        env:
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
          GOOGLE_API_KEY_2: ${{ secrets.GOOGLE_API_KEY_2 }}
          GOOGLE_API_KEY_3: ${{ secrets.GOOGLE_API_KEY_3 }}
        run: |
          python scripts/ai_review_from_rust.py output/bridge_scan.json output/ai_scan.json

      - name: 4. Generate Blog Posts
        run: |
          python scripts/generate_blogs_from_analysis.py output/ai_scan.json

      # NOTE: Image generation disabled until Gemini billing is activated
      # Uncomment after activating billing at https://console.cloud.google.com/billing
      # - name: 5. Generate Images with Gemini API
      #   env:
      #     GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
      #     GOOGLE_API_KEY_2: ${{ secrets.GOOGLE_API_KEY_2 }}
      #     GOOGLE_API_KEY_3: ${{ secrets.GOOGLE_API_KEY_3 }}
      #   run: |
      #     python scripts/generate_blog_images.py --limit 5
      #   continue-on-error: true

      - name: 5. Generate SVG Placeholders Only
        run: |
          echo "📐 Using SVG placeholders (Gemini billing not active)"
          python scripts/generate_placeholder_headers.py

      # - name: 6. Sync Images to Public
      #   run: |
      #     python scripts/sync_blog_images.py

      - name: 6. Organize Blog Posts
        run: |
          python scripts/organize_blog_posts.py

      - name: Commit and Push Changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"

          # Add new blog posts (including organized folders and images)
          git add website/src/content/blog/

          # Add output artifacts
          git add output/*.json

          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "🚀 Auto-generated blog posts + images via Rust Pipeline"
            git push
          fi
