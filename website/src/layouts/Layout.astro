---
interface Props {
	title: string;
	description?: string;
	image?: string;
	article?: {
		publishedTime?: string;
		modifiedTime?: string;
		author?: string;
		tags?: string[];
	};
	type?: 'website' | 'article';
}

const {
	title,
	description = "Discover the best open source projects. AI-curated directory of trending GitHub repositories with in-depth reviews, insights, and developer resources.",
	image,
	article,
	type = 'website'
} = Astro.props;

import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';
import SEO from '../components/SEO.astro';
---

<!doctype html>
<html lang="en" class="dark">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="icon" type="image/svg+xml" href="/bestof-opensorce/favicon.svg" />
		<link rel="apple-touch-icon" sizes="180x180" href="/bestof-opensorce/apple-touch-icon.png" />
		<meta name="generator" content={Astro.generator} />

		<!-- SEO Component with all meta tags -->
		<SEO
			title={`${title} | BestOf OpenSource - Best Open Source Projects Directory`}
			description={description}
			image={image}
			article={article}
			type={type}
		/>

		<!-- AI/LLM Access Control - Commercial use requires license -->
		<meta name="ai-content-license" content="commercial-use-requires-payment" />
		<meta name="ai-terms" content="https://iberi22.github.io/bestof-opensorce/api-pricing" />
		<link rel="ai-agent-manifest" href="/bestof-opensorce/agent.json" type="application/json" />
		<link rel="alternate" href="/bestof-opensorce/.well-known/ai-plugin.json" type="application/json" title="AI Plugin Manifest" />

		<!-- Fonts -->
		<link href="https://fonts.googleapis.com/css2?family=Fira+Code:wght@300;400;500;600;700&family=Outfit:wght@300;400;500;600;700&display=swap" rel="stylesheet">
		<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Source+Serif+4:ital,opsz,wght@0,8..60,400;0,8..60,500;0,8..60,600;0,8..60,700;1,8..60,400;1,8..60,500&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">

		<!-- Google AdSense -->
		<script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-7015371704987876" crossorigin="anonymous"></script>

		<!-- Google Translate API (hidden) -->
		<script type="text/javascript" src="//translate.google.com/translate_a/element.js?cb=googleTranslateElementInit"></script>
	</head>
	<body class="bg-dark-bg text-bone font-sans min-h-screen relative overflow-x-hidden">
		<!-- Google Translate Element (hidden) -->
		<div id="google_translate_element"></div>

		<!-- Noise Overlay -->
		<div class="fixed inset-0 z-[-1] opacity-[0.05] pointer-events-none bg-dark-bg" style="filter: url('#noiseFilter');"></div>
		<svg class="hidden">
			<filter id="noiseFilter">
				<feTurbulence type="fractalNoise" baseFrequency="0.6" stitchTiles="stitch" />
			</filter>
		</svg>

		<Navbar />
		<slot />
		<Footer />

		<script>
			// Automatic language detection and translation
			function googleTranslateElementInit() {
				// Get user's browser language
				const browserLang = navigator.language.split('-')[0]; // e.g., 'es', 'fr', 'de'
				const supportedLanguages = ['es', 'fr', 'de', 'pt', 'it', 'nl', 'pl', 'ja', 'zh', 'ar', 'ru'];

				// Only auto-translate if not English and language is supported
				if (browserLang !== 'en' && supportedLanguages.includes(browserLang)) {
					// Initialize translator but keep it hidden
					new google.translate.TranslateElement(
						{
							pageLanguage: 'en',
							autoDisplay: false,
							layout: google.translate.TranslateElement.FloatPosition.TOP_RIGHT
						},
						'google_translate_element'
					);

					// Hide the translator widget
					setTimeout(() => {
						const gTranslateWidget = document.getElementById('google_translate_element');
						if (gTranslateWidget) {
							gTranslateWidget.style.display = 'none';
						}

						// Auto-translate to detected language
						const selectElement = document.querySelector('select.goog-te-combo');
						if (selectElement) {
							selectElement.value = browserLang;
							selectElement.dispatchEvent(new Event('change'));
						}
					}, 500);
				}
			}
		</script>
	</body>
</html>

<style is:global>
	/* Custom scrollbar if needed */
	::-webkit-scrollbar {
		width: 8px;
	}
	::-webkit-scrollbar-track {
		background: #0A0A0A;
	}
	::-webkit-scrollbar-thumb {
		background: #2A2A2A;
		border-radius: 4px;
	}
	::-webkit-scrollbar-thumb:hover {
		background: #3A3A3A;
	}
</style>
